name: hive_demo
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  build_runner: ^2.0.4
  cupertino_icons: ^1.0.2

  # imports for hive
  hive: ^2.0.4
  hive_generator: ^1.1.0
  path_provider: ^2.0.2
  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
